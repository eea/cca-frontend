services:
  backend:
    build: ./eea.docker.plone-climateadapt
    entrypoint: sh -c "tail -f /dev/null"

  converter:
    build: ./converter
    volumes:
      - ./converter/app:/app/app
      - ./converter/requirements.txt:/app/requirements.txt
    ports:
      - "8000:8000"
    entrypoint: sh -c "tail -f /dev/null"

  frontend:
    entrypoint: sh -c "tail -f /dev/null"
